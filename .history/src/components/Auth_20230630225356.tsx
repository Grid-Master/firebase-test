import { FC } from 'react';

const Auth: FC = () => {
  return (
    <div>
      <input placeholder="email" />
      <input placeholder="password" />
    </div>
  );
};

export default Auth;
